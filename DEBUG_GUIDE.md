# 도시 표시 문제 디버깅 가이드

## 개요
표시되지 않는 도시를 찾기 위해 브라우저 콘솔에서 사용할 수 있는 디버깅 함수가 추가되었습니다.

## 사용 방법

### 1. 특정 연도/월의 표시되지 않는 도시 찾기
```javascript
// 기본값: 2000년 1월
findHiddenCastles()

// 특정 연도/월 지정
findHiddenCastles(1200, 6)  // 1200년 6월
findHiddenCastles(918, 1)   // 918년 1월
```

### 2. **완전히 표시가 누락되는 성 찾기** ⭐ NEW
```javascript
// 어떤 시점에서도 표시되지 않는 성을 모두 찾기
findCompletelyHiddenCastles()
```

이 함수는 다음 조건에 해당하는 성을 찾아줍니다:
- 히스토리 레코드가 없음
- 모든 히스토리 레코드에 country_id가 없음

### 3. **모든 성의 상태를 종합적으로 검사** ⭐ NEW
```javascript
// 전체 성 데이터 감사 (문제점 일괄 파악)
auditAllCastles()
```

### 4. 특정 도시의 상세 정보 확인
```javascript
// "서울"이라는 도시의 상세 정보 출력
analyzeCastle("서울")

// "평양"이라는 도시의 상세 정보 출력
analyzeCastle("평양")
```

## 함수 설명

### `findHiddenCastles(year, month)`
- **목적**: 특정 연도/월에 표시되지 않는 도시 목록을 조회
- **반환값**: 
  - console에 테이블 형식으로 출력
  - 배열 객체 반환 (변수에 저장 가능)
- **체크 항목**:
  - 현재 시간에 맞는 히스토리 레코드 유무
  - 히스토리의 country_id 유무
  - 좌표(lat, lng)의 유효성

### `findCompletelyHiddenCastles()` ⭐ NEW
- **목적**: 어떤 시점에서도 표시될 수 없는 성을 찾기
- **반환값**: 
  - 완전히 표시 누락되는 성의 배열
  - 각 성별로 누락 사유 포함
- **체크 항목**:
  - 히스토리 레코드 존재 여부
  - 모든 히스토리의 country_id 존재 여부

### `auditAllCastles()` ⭐ NEW
- **목적**: 모든 성의 데이터 품질을 종합적으로 검사
- **반환값**: 
  - 감사 결과 객체
  - 총 성 개수, 정상/문제가 있는 성 분류
- **분류**:
  - **정상 표시**: 모든 시간에 문제없이 표시됨
  - **시간별 표시**: 시간에 따라 표시됨
  - **완전 누락**: 어떤 시점에서도 표시 안 됨
  - **좌표 누락**: 좌표(lat/lng) 정보 없음

### `analyzeCastle(castleName)`
- **목적**: 특정 도시의 전체 히스토리와 문제점 분석
- **반환값**: 도시 객체
- **출력 정보**:
  - 도시 ID
  - 기본 국가 ID
  - 좌표
  - 모든 히스토리 레코드의 시간 범위와 소속 국가

## 예시 출력

### findCompletelyHiddenCastles() 실행 결과
```
========== 완전히 표시되지 않는 성 ==========
총 성: 150
완전히 표시 누락: 3
┌───────────┬──────────┬────────────────────────┐
│ 성명       │ 히스토리수│ 이유                    │
├───────────┼──────────┼────────────────────────┤
│ 서울      │ 0        │ 히스토리 레코드가 없음
│ 평양      │ 2        │ 모든 히스토리에 country_id가 없음
│ 개경      │ 1        │ 모든 히스토리에 country_id가 없음
└───────────┴──────────┴────────────────────────┘
```

### auditAllCastles() 실행 결과
```
========== 성 데이터 감사 보고서 ==========
총 성: 150
✓ 정상 표시: 145
⚠ 시간별 표시: 2
✗ 완전 누락: 3
✗ 좌표 누락: 0

문제가 있는 성:
┌────────┬──────────────────────┐
│ name   │ issues               │
├────────┼──────────────────────┤
│ 서울   │ 히스토리 없음        │
│ 평양   │ country_id 없음      │
└────────┴──────────────────────┘
```

### findHiddenCastles() 실행 결과
```
========== 2000년 1월 기준 ==========
총 도시: 150
표시되지 않는 도시: 5
┌───────────┬──────────┬─────────────────────────┐
│ 도시명     │ 히스토리수│ 문제점                   │
├───────────┼──────────┼─────────────────────────┤
│ 서울      │ 3        │ 현재 시간에 맞는 히스토리가 없음
│ 평양      │ 0        │ 현재 시간에 맞는 히스토리가 없음
└───────────┴──────────┴─────────────────────────┘
```

### analyzeCastle("서울") 실행 결과
```
========== "서울" 상세 정보 ==========
ID: 507f1f77bcf86cd799439011
기본 국가 ID: 507f1f77bcf86cd799439012
좌표: (37.5665, 126.9780)

히스토리 목록:
  [1] 918년 1월 ~ 1392년 12월
       국가: 507f1f77bcf86cd799439013
       이름: 한양(고려)
  [2] 1392년 1월 ~ 1910년 12월
       국가: 507f1f77bcf86cd799439014
       이름: 한양(조선)
  [3] 1945년 1월 ~ 현재
       국가: 507f1f77bcf86cd799439015
       이름: 서울
```

## 문제 해결 단계

### 🔴 완전히 표시 누락되는 성 해결하기

1. **findCompletelyHiddenCastles()** 실행
2. 누락 사유 확인:
   - **히스토리 레코드가 없음**: 해당 성의 히스토리 데이터 추가 필요
   - **모든 히스토리에 country_id가 없음**: 각 히스토리 레코드에 국가 ID 입력 필요
3. **analyzeCastle('성명')** 로 상세 정보 확인
4. 편집 폼에서 누락된 정보 추가
5. 브라우저 새로고침 후 **findCompletelyHiddenCastles()** 다시 실행하여 확인

### ⚠️ 특정 시점에 표시 안 되는 성 해결하기

1. **findHiddenCastles(year, month)** 실행하여 문제 시점 파악
2. **analyzeCastle()** 로 해당 성의 히스토리 확인
3. 문제점:
   - **히스토리가 없는 경우**: 데이터베이스에 히스토리 레코드 추가 필요
   - **히스토리의 country_id가 없는 경우**: 해당 히스토리 레코드의 country_id 입력 필요
   - **좌표가 없는 경우**: 위도/경도 데이터 입력 필요
4. 해당 성을 편집하여 누락된 정보 추가
5. 브라우저 새로고침 후 다시 확인

### 📊 종합 검사로 빠르게 파악하기

1. **auditAllCastles()** 실행
2. 결과에서 문제가 있는 성의 목록 확인
3. 우선순위대로 수정:
   - 먼저: 완전 누락 성 수정
   - 다음: 좌표 누락 성 수정
   - 마지막: 시간별 표시 성 검토

## 팁

- 콘솔에 변수를 할당하여 결과를 저장할 수 있습니다:
  ```javascript
  const hidden = findCompletelyHiddenCastles()
  const audit = auditAllCastles()
  ```
- 특정 성만 필터링하려면:
  ```javascript
  const hidden = findCompletelyHiddenCastles()
  console.log(hidden.filter(c => c.name.includes("경")))
  ```
- CSV 형식으로 내보내려면:
  ```javascript
  const hidden = findCompletelyHiddenCastles()
  console.log(hidden.map(c => `"${c.name}","${c.reason}"`).join('\n'))
  ```
