# 국력 대시보드 (prototype-2 잠재력 모델)

## 영토 중심 통합 (2026-09-20)

### 대한민국 최근 주민등록 인구

국력 API는 대한민국 지도 권역 17곳에 대해 행정안전부 주민등록인구의 2025년 12월·2026년 8월 말 수치를 적용한다. 근거와 영토 ID별 값은 `lib/korea-resident-population.json`에 있으며, 각각 전국 합계 51,117,378명·51,084,159명과 일치한다. 2026년 전남광주통합특별시 수치는 공식 시군구 자료의 광주 5개 구를 광주 지도 권역으로 합산하고 나머지를 전남 지도 권역으로 배정했다. 주민등록인구는 외국인을 포함하지 않으며, 2026년의 모든 월 표시는 8월 말 관측치를 사용한다. 역사 인구 시계열과 관리자 수동 보정은 보존한다. 국가 합계는 선택 시점에 해당 영토가 대한민국에 귀속된 범위에 따라 달라질 수 있다.

- 인구·입지·성/도시의 공간 배정은 국가 ID와 무관하다. 점자료 중첩은 최소 경계 상자 면적, 동일 면적이면 지역 ID 순으로 한 번만 배정하고 감사 목록에 기록한다. 영토 획득은 지역 수치를 유지한 채 국가별 합계만 바꾼다.
- 국력 패널은 `/api/power-regions`의 전체 비숨김 영토와 `/api/power-markers`의 전체 마커를 읽는다. 계산용 경계는 기존 지도 타일에서 생성한 `public/power-regions.json`이며 큰 경계 고리를 최대 약 150점으로 제한한다. DB 원본과 지도 편집 정밀도는 변경하지 않는다. DB에서는 현재 인구·생산·보정 메타데이터만 병합하므로 60MB 원경계를 매번 전송하지 않는다. 경계 편집 후 `node scripts/build-power-regions.js`로 계산 경계를 갱신해야 한다.
- 기존 호구수 레이어도 `NationalPower.populationLayer()`의 같은 폴리곤 집계값을 사용한다. 대표 위치는 경계 상자 중앙이며, 실제 도시 좌표를 뜻하지 않는다. 표시는 인구 ÷ 5의 **추정 호수**이고 사료 관측 호수가 아니다.
- 성(`seong`, `castle`, `fortress`, `hwangseong`), 도시(`city`, `capital`, `hwangseong`, 활성 연혁의 수도)를 구분한다. 주·군·현·진·일반 마커는 **기타 거점**이다. 자연지물·지명·군대·전투·시대 미확인 마커는 제외한다. 성/도시 이중 유형은 전체 거점에서 한 번만 센다. 같은 ID의 중복 마커도 한 번만 센다. 같은 장소의 서로 다른 ID 중복은 별도 정제가 필요하다.
- 비교 페이지의 표·지표 선택·CSV와 지역 GeoJSON에 성·도시·전체 거점 수를 추가했다. 등록 마커 수는 사서 수치 자체가 아니다.

### 영토 필드와 우선순위

1. `population_overrides`: 해당 기간의 마지막 수동 보정. `value`는 직접 인구, `multiplier`는 자동 추정에 곱할 배율. `mode: reset`은 이전 보정을 지우지 않고 해당 기간을 자동값으로 복원한다.
2. `population_series`: 영토 전체 인구 시계열.
3. `modern_population` + `population_scale`: 현대 기준 인구 × 연도별 계수. 계수가 없으면 기존 현대 도시 시드의 `legacyEraScale`을 사용한다. 이는 검증 전 가정이며 기본 지원 기간은 -200~2025년이다. 1800·1850·1900·1950·2000·2025년 명시 계수 사이만 선형 보간하며, 범위 밖은 임의 외삽하지 않는다.
4. 기존 인구 포인트 공간 연결(`pop_by_year`, 또는 `modern_pop` × 시대계수).

직접 지정 인구는 다른 지역을 자동 재분배하지 않는다. 권역 총인구 고정 배분은 아직 구현하지 않았다. 미확인 영역에 근거 없는 양수를 채우지 않는다. 기존 115개 현대 도시 인구 포인트는 전 영토를 덮는 인구 격자가 아니므로, 전체 분포 완성을 위해 별도의 격자/지역별 현대 인구 입력이 필요하다.

`production_series: {grain: {unit: "kg/year", values: {"1000": 10000, "1100": 12000}, source: "출처"}}` 형식의 물량을 읽으며 같은 자원·단위끼리 국가별 합산한다. 결측 물량과 입지 개수는 구분한다. **생산력 점수는 인구 기반 잠재량**이며 새 물량을 전투력으로 바꾸는 계수는 아직 없다.

### 관리자 인구 보정

국력 → 지역 → **영토 인구 수동 보정 · 관리자**에서 기간/값/사유를 입력한다. `PATCH /api/territories/:id/population`은 기존 관리자 인증을 사용하고, 정수 인구·기간·배율을 검사한다. `population_revision`으로 동시 수정을 거부하고 이전 보정, 수정 시각, 관리자 ID를 보존한다. 원 인구·자원 컬렉션과 경계·귀속은 변경하지 않는다. 미리보기는 저장 불가다.

`node scripts/audit-national-power.js`는 DB를 읽기만 하는 공간 커버리지 검사다. 역사 국가 귀속을 추측하지 않도록 검사 전용 단일 소유자를 사용한다. 실서비스 DB에 시험 보정을 저장하거나 삭제형 시드 스크립트를 실행하지 않았다.

## 비교 페이지와 인구 누락 진단 (추가)

국력 패널 **비교·추이 ↗**는 `power-comparison.html`을 전체 화면으로 연다. 원본 지도는 유지되며 같은 출처의 메시지 채널로 선택 기간의 연도별 집계를 전달한다. 링크를 직접 열면 원본 지도 연결 안내가 나온다. `?demo=1`은 명시적인 가상 자료 모드다. 기간은 최대 101시점이며 인구의 절대 수량과 기간 공통 기준의 국력/군사/생산 지수를 각각 그래프로 표시한다. 결측 시점은 0으로 채우거나 선으로 이어 붙이지 않는다. 비교 기준이 기간 전체의 최대값이므로 지도 마커의 단일 연도 상대점수와 다를 수 있다.

**자료 누락** 탭은 국가 귀속이 없는 폴리곤까지 포함한다. 인구 미연결, 해당 연도 자료 없음, 영토 중첩, 일부 기록만 합산된 상태를 구분하고 제외된 원자료의 사유를 표시한다. 지역 행을 누르면 지도에서 위치를 찾는다. 누락 지역 레이어는 원래 국가 색상을 유지하면서 별도 윤곽과 반투명 색으로 겹쳐 표시한다. 붉은색은 인구 미확인, 주황색은 부분/충돌, 회색은 국가 귀속 미확인이다.

폴리곤별 인구는 `region_id + year`로 연결하는 것이 기본이다. 인구는 정권이 바뀌어도 지역에 남는다. 국가는 화면 색상이 아니라 그 색상을 결정한 해당 연월의 `countryId`로 집계한다. `resources.region_id` 또는 `territory_id`가 있으면 명시적 연결을 우선하고, 없으면 좌표로 공간 결합한다. 명시적 ID가 현재 영역에 없으면 다른 지역으로 추측 배정하지 않는다. 날짜 없는 단일 인구를 전 시대에 적용하지 않는다.

선택적으로 영토의 `population_series: {"1000": 12345, "1100": 23456}`와 `population_source`를 읽을 수 있다. 이 값은 전체 지역 인구로 취급하므로 같은 지역의 인구 점자료보다 우선한다. 겹치는 상위/하위 행정구역 전체인구를 함께 입력하면 중복 집계될 수 있으므로, 입력 단위는 서로 겹치지 않는 기본 지역으로 정해야 한다. 기존 자료를 자동으로 덮어쓰지 않는다.

**인구 GeoJSON ↓**는 각 폴리곤의 properties에 `year`, `country_id`, `population`, `population_status`, `population_reason`, `population_origin`, `ownership_status`를 담아 내보낸다. 자료가 없는 인구는 0이 아니라 null이다. 로드된 폴리곤 중 자료가 연결된 수는 자료 커버리지일 뿐 통계적 신뢰도나 전국 인구를 의미하지 않는다.

3D 지도 `index.html`의 자원 버튼 옆에 **⚔ 국력**을 추가한다. `?power=1`로 바로 열 수 있다. 국력 버튼은 국가/지역 상세, 국가 비교, 인구·군사·생산 지수, 국가명 하단 게이지를 제어한다. 지도 클릭 또는 지역 목록으로 지역 상세를 선택한다.

## 데이터

`/api/resources?type=population,iron,horse,salt,silk,gold`를 읽기 전용으로 사용한다. 전체 영토와 지도 메모리의 `countries`, `getCachedDominantCountry`를 통해 선택 연월의 귀속을 연결한다. 영토가 모두 로드되어도 인구 근거가 연결되지 않은 곳이 있으므로 전국 완전 통계로 해석할 수 없다. 패널에서 새로고침하면 영토·마커·자원 자료를 다시 읽는다.

인구 `pop_by_year`는 인접 연도 사이를 선형 보간하며, 자료의 시간 범위를 벗어나면 null이다. `est_population` 단독 값은 해당 `target_year`가 있을 때만 사용한다. 기존 `geo_cluster_v3`는 사료 관측이 아닌 모델값이다. 자원은 등록된 입지 개수이며 연간 생산량·매장량·군마 두수가 아니다. `start_year`, `end_year`가 있으면 기간을 적용한다. 날짜 없는 입지의 포함 여부는 설정에서 전환한다.

각 자원은 `region_id` 또는 점-폴리곤 포함 판정으로 한 지역에만 배정한다. 중첩은 국가와 무관한 최소 경계 상자/지역 ID 규칙으로 배정하고 중첩 건수로 표시한다. 국경의 진위를 검증하는 모델이 아니며 현재 지도 시나리오의 귀속을 사용한다.

## 계산

- 지역 기본 생산 잠재량 = 연결 인구 × 지역 생산성 계수(기본 1, 허용 0.5–1.5)
- 거점 보너스 = min(기본 생산 잠재량 × 20%, 도시 × 1,000 + 성 × 300 + 기타 거점 × 100). 실제 생산량이 아닌 인구환산 모델 단위다.
- 동원 후보 = 연결 인구 × 사용자 동원율(기본 3%, 범위 1–5%)
- 군사 잠재량 = 동원 후보 × 훈련 계수(0.7–1.3) × 보급 계수(0.5–1.2) + 상한 있는 말·방어 보너스
- 생산력·군사력·거점·자원 입지는 각각 `100 × log(1+값) / log(1+비교 기준 최대값)`으로 정규화한다. 국가와 지역은 각각 비교하고, 기간 그래프는 기간 전체의 고정 기준을 쓴다.
- 총 국력 = 생산력 45% + 군사력 35% + 거점 10% + 자원 입지 10%. 인구를 세 번째로 더하지 않는다.

### 현대 남북 경제 보정과 설명 자료

2024년 대한민국·북한 국가 생산력은 같은 기준의 명목 GDP(각 2,556.9조 원·43.7조 원), 2025년은 한국은행 명목 GNI(각 2,717.1조 원·48.5조 원)의 제곱근 상대지수로 보정한다. 2026년은 2025년 최신 관측치를 **준용**한다고 표시한다. 2024 GDP의 출처는 [통계청 남북 비교](https://www.kostat.go.kr/boardDownload.es?bid=11825&list_no=442558&seq=2), 2025 GNI는 [한국은행 남북 주요경제지표 비교](https://www.bok.or.kr/portal/bbs/P0002240/view.do?menuNo=200092&nttId=10085022)다. 둘은 서로 다른 지표이므로 연도 간 증가율로 연결하지 않는다. 이 보정은 국가 단위이며 영토 인구·생산량 원자료를 바꾸지 않는다. 다른 나라의 인구 기반 잠재력과 완전히 동일한 통계 기준이 아니므로 전체 세계 순위 해석에는 주의가 필요하다.

`power-reference-data.js`의 무기·함종·주요 생산품은 국가 상세와 비교 페이지의 **설명 자료**로만 표시한다. 운용 수량이 확인되지 않은 항목은 검증 대기로 표시한다. 무기 명칭만으로 군사력·총 국력에 가산점을 주지 않는다. 고려의 청자·종이·나전칠기는 출처 링크를 제공하며, 비단·말·맹화유는 생산 지역·수량·분류 검증 전의 후보로 둔다.

국가 상세 화면은 연속 시점 사이 추정 인구가 18% 이상, 5천 명 이상 줄면 해당 점을 강조하고 영토 귀속·연결 지역 감소 또는 다음 시점 반등 여부를 점검 경고로 보여준다. 같은 시기의 사서 사건은 참고 목록이며, 그 사건이 수치 급락의 원인이라는 뜻은 아니다.

관리자는 지도 → 국력 → 국가 상세에서 **✎ 상세 자료 수정** 버튼을 눌러 무기·생산품·사건의 각 입력 칸을 직접 고치고 항목을 추가·삭제한 뒤 저장할 수 있다. 인구 스프레드시트는 영토 인구 원자료를 편집한다. 기본 예시 자료를 고치면 국가별 전체 설명 목록으로 저장되므로 기존 항목을 보존하려면 그대로 두어야 한다. 조선의 임진왜란·정묘호란·병자호란·갑오개혁·대한제국 선포가 사건 예시로 등록되어 있다.

### 사건 인구 보정

사건은 기본적으로 설명 자료다. 관리자가 사건에 인구 변화율(−100~+100%, 소수 첫째 자리), 효과 시작·종료 연도, 변화율 산정 근거, HTTPS 출처를 등록한 경우에만 `power_profile_overrides.events`의 인구 효과로 사용한다. 영토 선택은 선택 사항이다. 영토를 고르면 지정 영토에만 적용하고, 선택하지 않으면 해당 연도에 그 국가에 귀속된 모든 영토에 **같은 비율**을 적용한다. 따라서 인구가 많은 영토의 절대 감소 인원도 더 많아지며, 인구를 지역마다 같은 인원으로 차감하지 않는다. 국가 전체 방식은 매 연도 영토 귀속을 다시 보므로 영토가 다른 국가로 바뀌면 그 효과 대상에서도 빠진다. 기본 예시 사건에는 임의의 감소율을 넣지 않는다.

영토 기준 인구가 있는 연도에만 `반영 인구 = round(기준 인구 × (1 + 변화율/100))`을 계산한다. 사건 보정은 국가 이름·현재 소유자와 무관하게 선택된 영토 ID에 적용한다. 자료가 없는 연도의 인구를 만들지 않는다. 같은 영토·기간에 여러 사건 효과를 등록하려 하면 저장을 거부한다. 기존 자료에 중복 효과가 있다면 모델은 마지막 효과만 적용하고 집계 문제 목록에 경고를 남긴다. 이미 등록된 영토 수동 보정은 사건보다 우선하며, 사건의 적용 여부를 지역 상세에서 확인할 수 있다. 보정 전 기준 인구는 별도로 보존하고, 국가 상세 인구 그래프에 보정 전·후를 함께 표시한다. 현재 1차 방식은 지정 기간에 일정 비율을 즉시 적용하고 기간이 끝나면 해제한다. 전후 회복 곡선이나 사건 간 누적 피해를 자동 추정하지 않는다.

모든 가중치와 거점 환산치는 UI 검증용 가정이다. 군사력은 실제 상비군·전쟁 승률이 아니다. 동원율은 동원 후보와 군사 잠재량에 반영된다. 국가는 지역 잠재량을 먼저 합산하고 정규화하며 지역도 별도 집단에서 정규화한다. 국가점수와 지역점수를 합산하거나 서로 직접 비교하지 않는다. 인구 결측이면 점수를 만들지 않는다. 표의 국가 순위는 인구가 연결된 국가들 사이에서만 유효하다. 자원 0은 등록 입지 0건을 의미한다.

## 미리보기 및 확인

`power-preview.html`은 DB 없이 화면을 확인하는 가상 데이터 전용 페이지다. 지도도 설명용 개략도이며 실제 역사 경계가 아니다. 실제 지도는 이 예시 값을 사용하지 않는다.

`node scripts/test-national-power.js`로 보간·범위·결측·공간 중복·귀속 충돌·시대 필터·동원율을 확인한다. 별도 DB 생성·자료 쓰기·기존 시드 스크립트 실행은 하지 않는다.

## 실제 생산량 모델로 확장할 데이터 계약

사용자가 제시한 네 축은 다음 단위로 연결한다. 현재 UI의 입지 기반 지수와 실제 수량을 혼용하지 않는다.

| 축 | 추가할 원자료 | 처리 원칙 |
| --- | --- | --- |
| 공간 | 지역 ID, Polygon, 강·산맥, 토질, 경지·초지 면적 | 경계 버전과 시나리오 ID 유지 |
| 시대·국가 | 정권 ID, 귀속 시작·종료 연월, 비정 근거 | 실제 점유·영향권·가설 구분 |
| 생산 | 경지 ha, 수확량 kg/ha/년, 철 kg/년, 소금 kg/년, 비단 원단 단위, 군용 말·소 두수 | 원문 단위와 환산 계수의 시대·지역 근거 보존 |
| 인적·군사 | 호수, 호종, 총인구, 정남, 군적, 군량, 지급 가능한 장비 | 중복 호적 제거, 정남과 실제 동원군 구분 |

호당 4.5–5.5명과 정남 비율 20–25%는 사용자가 제시한 시나리오 가정이며, 지역·시대별 보정값으로 관리한다. 정남 전체를 상비군으로 간주하지 않는다. 결(結)은 일률적인 면적 단위로 환산하지 않고, 석(石)·근(斤)도 시대·지역별 환산 근거가 있어야 표준 단위로 변환한다.

공급 기반 병력 상한은 인구 제약, 군용 식량 잉여 / 병사당 소비량, 공급 가능한 장비 / 병사당 장비 요구량의 최솟값으로 계산한다. 캠페인 기간과 민간 소비·종자·사료·수송 손실을 먼저 차감해야 한다. 기병 상한에는 훈련된 기수·군용 말·교체마·사료가 모두 필요하다. 입력이 없으면 해당 상한은 **미산출**로 유지한다. 농업은 면적 × 수확량 × 수확 횟수, 목축은 초지 면적 × 수용력으로 잠재량을 추정하되 기후·관개·기술·이용 가능성을 별도 가정으로 기록한다. 현재 prototype-1에는 이 물리량 모델과 실증 사료 수집이 아직 포함되지 않았다.
